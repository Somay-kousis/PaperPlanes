"""LongMemEval-style probes for PaperPlanes' memory engine.

See ``tests/eval/README.md`` for how to run this suite and what each
probe demonstrates. Entrypoint: ``python -m tests.eval.run_eval`` (run
from ``backend/``).
"""
